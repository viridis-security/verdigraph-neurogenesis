.venv/
__pycache__/
*.pyc
.pytest_cache/
dist/
build/
*.egg-info/
examples/output/
.DS_Store
.env
venv/
env/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/
.idea/
.vscode/
*.swp

# Private strategy / internal-only docs (never commit)
STRATEGY.md
docs/internal/

# Zenodo deposit staging — build artifact, regenerated as needed
zenodo_deposit/

# Operator agent state backups — per-run snapshots, not source of truth
verdigraph_state/*.bak-*

# Cowork-imported scaffold (project instructions live in CLAUDE.md, not here)
AGENTS.md

# PR drafts — local scratch, opened manually
pr-drafts/
